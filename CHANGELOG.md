# 변경 기록

이 파일은 배포되는 버전마다 무엇이 달라졌는지 남긴다. 버전은 `package.json`의 `version`을
따르고, 설정 페이지 하단에 같은 값이 표시된다.

형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르며
[유의적 버전](https://semver.org/lang/ko/)을 쓴다.

## [1.0.4] - 2026-09-14

### 추가

- **세대표**: 동 삭제(하위 층·세대·체크·미타공 기록은 함께 지워지고 되돌릴 수 없다는
  경고 후 진행), 동 순서를 드래그로 재배열, 우상단에 가로/세로 보기 전환 버튼(동을
  좌우로 스크롤하는 가로 모드 추가).
- **현장 목록**: 완료 세대 옆에 완료율(%)을 함께 표시. 상단에 현장명 검색창 추가.
- **미타공**: 위치에 전체 선택 체크박스를 추가하고, 위치를 여러 개 고르면 하나로
  묶지 않고 위치마다 별도 건으로 나눠 등록한다. 내용은 복수 선택에서 하나만 고르는
  방식으로 바꿨다.
- **현장 상세**: 최상단에 세대표/체크리스트 전환 탭을 추가. 체크리스트는 자유
  텍스트로 작업 항목을 적고 체크박스로 완료 처리하며, 마지막으로 체크한 사람과
  시각을 함께 보여준다.
- **결제**: 세대가 전부 체크되어 "완료"로 뜨는 현장을 모아 보는 "완료 현장" 탭을
  추가했다. 인건비 옆에는 "비용 통계" 탭을 추가해 월 단위/연 단위로 매출(계약금액·
  수령금액)·매입(급여·지출)·단기 순이익을 보고, 각 카드를 누르면 그 계산에 쓰인
  현장별·인원별 내역과 합계를 바로 아래에서 확인할 수 있다. 인건비 상세 대시보드에는
  선택한 연/월의 차액·지출금액 카드를 추가했다.
- **인사관리**: 단가를 적용 연/월과 함께 수정한다. 이제 단가를 바꿔도 과거 달의
  급여·차액은 그 당시 단가로 그대로 남고, 새로 지정한 연/월부터만 적용된다(예약
  적용·소급 정정 모두 가능). 같은 달에 다시 고치면 그 값으로 덮어쓴다.
- **개인**: 결제·인사관리·개인 메뉴 전체가 연/월 선택 상태를 공유해서, 메뉴를
  옮겨도 오늘 날짜로 초기화되지 않는다. 출근체크에서 오전·오후를 서로 다른
  현장으로 각각 반일씩 등록할 수 있다(하루 종일 체크는 그대로 유지).
- **로그인**: 이메일 인증이 완료되지 않았다는 에러가 뜨면 인증 메일을 다시 보낼 수
  있는 버튼이 나타난다.

### 수정

- 큰 현장일수록 완료 세대수가 실제보다 훨씬 적게 뜨던 문제. PostgREST가 한 번에
  최대 1000행만 돌려주는데 그 이상 쌓인 현장의 체크 기록이 조용히 잘려서 생긴
  버그였다. 세대표 공유 기능 자체는 문제가 없었다.
- 세대표에서 없는 세대(빗금)가 기기의 강제 다크모드에서 잘 안 보이던 문제.
- 인원별 차액을 세전 값이 아니라 기본공제 3.3%를 뗀 96.7%(소수점 버림)로 표기하도록
  계산 기준을 바꿨다.

### 보안

- 팀원이 devtools로 자기 단가를 직접 바꿀 수 있던 구멍을 막았다. 단가 변경은
  `update_profile_rate` RPC로만 가능하다.
- 영수증 스토리지 조회 정책을 "로그인만 하면 전체 열람 가능"에서 본인 소유이거나
  매니저인 경우로 좁혔다.

### 참고

- 스키마 변경 있음. `profile_rate_history`(단가 이력), `site_checklist_items`(체크리스트)
  테이블을 추가했고, `buildings.sort_order`, `attendances.half`(AM/PM/FULL) 컬럼을
  추가했다. `update_profile_rate` RPC가 (대상, 단가) 2개 인자에서 (대상, 단가, 연,
  월) 4개 인자로 바뀌었다 — 이전 버전 프론트엔드와는 호환되지 않는다.
- 기존 0.5일 출근 기록은 오전/오후 구분이 없었으므로 오전(AM)으로 이관했다(실제
  근거는 없는 최선의 근사치).

## [1.0.3] - 2026-09-13

### 추가

- 세대표에 세대 타입을 넣는다. 세대표 수정에서 호마다 `84A` 같은 평형 표기를 입력하면
  열 머리의 호 번호 아래에 작은 글씨로 함께 보인다. 타입을 쓰지 않는 현장은 비워두면
  호 번호만 나온다. 세대 상세 패널 제목에도 `101동 3호(84A) 5층`처럼 붙는다.
- 세대표 수정에서 호마다 **시작층과 마지막층**을 따로 입력한다. 그동안 최대 층수만 받아
  모든 라인이 1층부터 시작한다고 봤기 때문에, 2층부터 올라가는 라인이 있는 동은 세대표를
  제대로 그릴 수 없었다. 마지막층이 시작층보다 낮으면 저장하지 않고 몇 호가 잘못됐는지
  알려준다.
- 작업 체크에서 칸을 끌어 **여러 칸을 한 번에 체크**한다. 경량·합지를 고른 뒤 끌면 그
  직사각형 범위가 한 번에 칠해진다. 체크할지 해제할지는 처음 누른 칸의 상태로 정해서,
  구간에 체크된 칸과 안 된 칸이 섞여 있어도 한 방향으로만 칠해진다.

### 수정

- 팀장이 인사관리에서 남의 단가를 고쳐도 저장되지 않던 문제. `profiles`의 수정 권한이
  "개발자만"과 "본인만" 두 가지뿐이라 RLS가 대상을 0건으로 걸러냈고, 오류도 뜨지 않아
  저장된 것처럼 보였다. 등급은 그대로 개발자만 바꾸게 두고 단가만 팀장도 바꿀 수 있도록
  열었다.
- 현장 목록의 총 세대수를 라인이 실제로 쓰는 층만 세도록 고쳤다. 완료 세대수도 지금
  세대표에 보이는 칸만 세서, 층 범위를 좁혔을 때 완료 수가 총 세대수를 넘지 않는다.

### 변경

- 세대표 열 이름을 `1세대`에서 `1호`로 바꿨다. 세대표 수정 화면과 세대 상세 패널
  제목에서도 같은 표기를 쓴다.
- 작업 체크 중에는 칸 위에서 끄는 동작이 선택으로 쓰인다. 화면을 옮길 때는 왼쪽 층 칸이나
  표 바깥 여백을 끌면 된다.

### 참고

- 스키마 변경 있음. `building_lines`에 `min_floor`(기본값 1)와 `unit_type`을 추가했다.
  기존 라인은 모두 1층부터였으므로 세대표는 이전과 똑같이 보인다. 단가 수정을 위한
  `update_profile_rate` 함수도 함께 추가했다.

## [1.0.2] - 2026-09-13

### 수정

- 세대표 수정에서 세대수(라인 수)를 고칠 수 없던 문제. 칸을 비우면 곧바로 1이 되살아나
  원하는 숫자를 넣을 수 없었고, 지우고 `12`를 치면 112가 되어 세대 칸 112개가 그려졌다.
  화면을 전체 선택해 덮어쓸 수 있는 PC와 달리 아이폰에서는 입력 자체가 막힌 것처럼 보였다.

### 변경

- 세대수·층수 칸을 입력하는 동안에는 빈 칸으로 두었다가 포커스를 뗄 때 정리한다. 숫자
  키패드는 그대로 뜬다.
- 한 동에 둘 수 있는 세대 수를 50으로 제한한다.
- 모바일에서 모달 입력창 글자를 16px로 키웠다. 아이폰이 입력창을 누를 때 화면을 확대하지
  않는다.

### 참고

- 스키마 변경 없음.

## [1.0.1] - 2026-09-13

### 추가

- 출근체크 달력에 어느 예정현장을 방문하는 날인지 표시한다. 기존에는 테두리로만
  표시돼 날짜만 알 수 있었는데, 출근 정보 아래에 "○○ 현장 방문 예정"을 빨간 글씨로
  붙인다. 하루에 여러 현장을 방문하는 경우도 모두 보여준다.
- 세대표 수정에서 이미 등록한 동의 이름·세대수·층수를 고칠 수 있다. 그동안 동 추가만
  가능했다.

### 변경

- 예정현장 연락처를 숫자만 입력받는다. 전화 앱에 그대로 넘길 수 있도록 하이픈·공백을
  걸러 저장하고, 화면에 보여줄 때만 `010-1234-5678`처럼 끊어준다. 예전에 하이픈째로
  저장된 연락처도 전화를 걸 때는 숫자만 넘어간다.
- 예정현장 목록을 다가오는 방문일 순으로 정렬한다. 지난 일정만 있거나 일정이 없는
  현장은 뒤로 보내고 등록 순서를 유지한다.

### 참고

- 세대수나 층수를 줄여도 체크·미타공 기록은 지워지지 않는다. 화면에서만 사라지고 다시
  늘리면 그대로 보인다. 수정 화면에서 이 점을 미리 알려준다.
- 스키마 변경 없음.

## [1.0.0] - 2026-09-12

첫 정식 버전. 그동안 `0.0.0`이던 버전을 정리하고 설정 페이지에 표시하기 시작했다.

### 추가

- **개인** — 대시보드, 출근체크(달력), 지출비용
- **현장관리** — 현장 목록, 세대표(작업 체크·미타공·석고 시공·작업 로그), 현장 소프트 삭제
- **결제** — 현장별 / 인건비 탭
- **인사관리** — 인원 목록과 상세
- **예정현장** — 방문 일자별 알림 설정
- **설정 / 개발자 페이지** — 내 정보 수정, 동기화, 앱 버전 표시
- **여러 현장이 세대표 하나를 공유** — 같은 도면을 쓰는 현장끼리 경량·합지·석고 시공·
  미타공 기록을 함께 본다. 공유 중임을 배너와 배지로 항상 보여준다.
- **오프라인 지원** — 세대표를 캐시해 오프라인에서도 열람할 수 있고, 출근체크·지출·
  세대표 편집은 큐에 쌓아뒀다가 온라인이 되면 순서대로 전송한다.
- **PWA** — 홈 화면 설치, Cloudflare Workers 배포용 매니페스트와 아이콘

### 권한

- 팀원 / 팀장 / 개발자 3단계. 권한 없는 메뉴는 렌더링하지 않고 DB의 RLS로 한 번 더
  막는다.

[1.0.4]: https://github.com/skditjdqja12/Integrated-Construction-Site-Management-App/releases/tag/v1.0.4
[1.0.3]: https://github.com/skditjdqja12/Integrated-Construction-Site-Management-App/releases/tag/v1.0.3
[1.0.2]: https://github.com/skditjdqja12/Integrated-Construction-Site-Management-App/releases/tag/v1.0.2
[1.0.1]: https://github.com/skditjdqja12/Integrated-Construction-Site-Management-App/releases/tag/v1.0.1
[1.0.0]: https://github.com/skditjdqja12/Integrated-Construction-Site-Management-App/releases/tag/v1.0.0

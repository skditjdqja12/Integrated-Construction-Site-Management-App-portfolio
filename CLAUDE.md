# 현장 관리 앱

## 프로젝트 개요
건설 현장 관리를 위한 사내 전용 PWA. 앱스토어 배포 없이 웹으로 직접 배포.

## 기술 스택
- React + Vite
- Supabase (DB/인증) — 클라이언트는 src/lib/supabase.js
- 배포: Cloudflare Workers (wrangler.jsonc, dist를 정적 자산으로 서빙)
- 스타일: 순수 CSS (프레임워크 없음)

## 설계도
`docs/현장관리어플_기능데모.html` 이 파일이 전체 기능 명세이자 UI 설계도다.
새 기능을 만들 때는 반드시 이 파일의 해당 부분을 먼저 확인할 것.

## 권한 체계
- 팀원 / 팀장 / 개발자 3단계
- 메뉴 접근: 개인·현장관리·설정(전체) / 결제·인사관리·예정현장(팀장,개발자) / 개발자페이지(개발자)
- 권한 없는 메뉴는 아예 렌더링하지 않는다
- DB 레벨에서도 RLS로 이중 보호됨

## 코딩 규칙
- 컴포넌트는 src/pages/ (페이지), src/components/ (공용)
- Supabase 쿼리는 src/api/ 폴더에 기능별로 분리
- 금액은 항상 정수(원 단위)로 저장하고, 표시할 때만 포맷팅
- 날짜는 'YYYY-MM-DD' 문자열로 통일
- 주석과 UI 텍스트는 한국어

## 하지 말 것
- .env 파일 내용을 코드에 하드코딩하지 말 것
- secret 키(sb_secret_ / service_role)는 절대 사용하지 말 것